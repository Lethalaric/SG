from random import randint
import collections

ind = collections.namedtuple('individu', 'ind')

def __init__(self, pop_total = 10)
	self.pop_total = pop_total

def fps (a)
	total = sum(a)	
	b = randint(0, total)
	awal = 0
	for x in a.item():
		awal += x
		if awal > b:
			return x			
			
"""
masih ga jelas
"""